{
  "name": "foo-workspace",
  "workspaces": [
    "wasm4-playground",
    "wasm4-runtime"
  ],
  "scripts": {
    "prep": "cd playground && npm install && cd ../wasm4-runtime && npm run prepare && npm install && npm run build",
    "dev": "cd playground && npm run dev",
    "build": "cd playground && npm run build"
  }
}